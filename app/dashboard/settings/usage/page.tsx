import { UsageView } from "@/components/UsageView";

// Usage: where the active agent's spend went this month. Its own sidebar entry directly under
// Credits (David's call) - Credits answers "how much is left", Usage answers "where did it go".
export default function Page() {
  return <UsageView />;
}
