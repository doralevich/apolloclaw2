import { PlanView } from "@/components/PlanView";

// The subscription — license, hosting seats, invoices. See PlanView for why this page exists
// and why the wallet stayed at /settings/billing.
export default function Page() {
  return <PlanView />;
}
